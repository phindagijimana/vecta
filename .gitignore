# Local-only — not part of the public DBI repository surface
plan/

# Runtime state for ./scripts/vecta (start/stop/logs)
.vecta/

# Legacy local folder (optional); package now lives at repo root
content/

# Byte-compiled / cache
__pycache__/
*.py[cod]
*$py.class
*.so

# Packaging / build
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
ENV/
env/

# Testing / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
hypothesis/

# Type checkers / linters (local)
.mypy_cache/
.ruff_cache/

# IDE / OS
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db

# Secrets / local env
.env
.env.*
!.env.example

# --- DBI: regenerated outputs (may contain paths; do not ship large NIfTI) ---
dbi/outputs/
dbi/outputs_phase4/
dbi/outputs_dcm2niix_v1freeze/
dbi/*.log

# Large conversion artifacts
dbi/outputs_dcm2niix_v1freeze/nifti/

# Local cohort roots (users mount their own data)
CIDUR_data/
CIDUR_BIDS/

# --- Local-only: manuscript, internal docs, one-off builders ---
writing/
review/
paper_repro/
build_*.py
*.docx
